import Loader from "@/components/Loader"

// Displayed while an individual event is resolved from Sanity.
export default function EventLoading() {
  return <Loader fullPage label="Loading event details" />
}
