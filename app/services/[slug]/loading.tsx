import Loader from "@/components/Loader"

// Displayed while a dynamic service page prepares its content.
export default function ServiceLoading() {
  return <Loader fullPage label="Loading service details" />
}
