import Loader from "@/components/Loader"

// Displayed while the About route prepares its page content.
export default function AboutLoading() {
  return <Loader fullPage label="Loading about page" />
}
